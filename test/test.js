const assert = require('assert');
/*const newUser = require('../routes/users').newUser();
const found = require('../routes/users').found();*/

describe('User', () => {
  it('should allow user to sign up', () => {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});

describe('Login', () => {
  it('should allow user to log in', () => {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});

describe('View', () => {
  it('should allow user/agent to view properties of a specific type', () => {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});

describe('Type', () => {
  it('should allow user/agent to view properties of similar type', () => {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});

describe('Update', () => {
  it('should allow user or agent to update properties', () => {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});

describe('Delete', () => {
  it('should allow user or agent to delete a property', () => {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});

describe('Post', () => {
  it('should allow user or agent post a property advert', () => {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});

describe('Mark', () => {
  it('should allow user or agent to mark property as sold', () => {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});
